# SQLite database (PC-specific data)
database/*.db
database/*.db-shm
database/*.db-wal

# PC-specific seed (use seed.example.php as template)
database/seed.php

# PC-specific overrides
CLAUDE.local.md
knowledge/
deployments.log
handoff-*.md

# Temp files
trello.zip
*.swp
*.swo

# Apache vhost (each PC sets up their own)
setup-vhost.sh
